import { MemberInfo } from '../interface';
export declare class SourceMemberConfig {
    readonly sourceMember: MemberInfo;
    private _ignored;
    constructor(sourceMember: MemberInfo);
    ignore(): void;
    isIgnored(): boolean;
}
