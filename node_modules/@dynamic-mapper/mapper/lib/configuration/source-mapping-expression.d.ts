import { ISourceMemberConfigurationExpression, MemberInfo } from '../interface';
import { TypeMap } from '../type-map';
import { ISourceMemberConfiguration } from './interface';
export declare class SourceMappingExpression implements ISourceMemberConfigurationExpression, ISourceMemberConfiguration {
    private readonly sourceMember;
    private readonly sourceMemberActions;
    constructor(sourceMember: MemberInfo);
    ignore(): void;
    configure(typeMap: TypeMap): void;
}
