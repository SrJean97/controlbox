import { IConfigurationProvider, MapperFunction } from '../interface';
import { TypeMap } from '../type-map';
export declare class TypeMapPlanBuilder {
    private readonly configurationProvider;
    private readonly typeMap;
    constructor(configurationProvider: IConfigurationProvider, typeMap: TypeMap);
    createMapper(): MapperFunction<any, any>;
    private createDestinationFunc;
    private createAssignment;
    private tryPropertyMap;
    private createTransformerFn;
    private mapFunction;
    private buildValueResolver;
}
