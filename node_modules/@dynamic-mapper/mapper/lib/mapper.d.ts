import { IRuntimeMapper } from './interface';
import { ResolutionContext } from './resolution-context';
import { MapperConfiguration } from './mapper-configuration';
import { MappingPair } from './mapping-pair';
export declare class Mapper implements IRuntimeMapper {
    private readonly configuration;
    readonly defaultContext: ResolutionContext;
    constructor(configuration: MapperConfiguration);
    map<TSource, TDestination>(pair: MappingPair<TSource, TDestination>, source: TSource, destination?: Partial<TDestination>): TDestination;
    map<TSource, TDestination>(pair: MappingPair<TSource, TDestination>, sources: TSource[], destination?: TDestination): TDestination[];
    private mapArrayToObject;
}
